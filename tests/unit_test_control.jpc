メイン｛
    ”カウント”を「0」で宣言する。
    
    「--- ループテスト ---」と出力する。
    ループ（”カウント”が「5」より小さいか）｛
        「カウント：”カウント”」と出力する。
        ”カウント”に「1」をたす。
    ｝

    「--- 条件分岐テスト ---」と出力する。
    ”値”を「10」で宣言する。

    もし（”値”が「10」と一緒か）｛
        「値は10です（正解）」と出力する。
    ｝
    ではない｛
        「値は10ではありません（不正解）」と出力する。
    ｝

    ”値”に「20」を代入する。
    もし（”値”が「10」と一緒か）｛
        「値は10です（不正解）」と出力する。
    ｝
    ではなく（”値”が「20」と一緒か）｛
        「値は20です（正解）」と出力する。
    ｝
    ではない｛
        「値は10でも20でもありません（不正解）」と出力する。
    ｝
｝
